import { Meta, Story, Canvas } from '@storybook/blocks'

import * as BoxStories from '../stories/Box.stories'

<Meta of={BoxStories} />

# Box

O componente de `Box` server para criar um container padrão para o conteúdo.

<Canvas of={BoxStories.Base} />
