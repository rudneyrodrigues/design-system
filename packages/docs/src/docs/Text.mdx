import { Meta, Story, Canvas } from '@storybook/blocks'

import * as TextStories from '../stories/Text.stories'

<Meta of={TextStories} />

# Text

O componente de `Text` é utilizado para exibir texto na tela.

<Canvas of={TextStories.Primary} />
