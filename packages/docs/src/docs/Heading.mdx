import { Meta, Story, Canvas } from '@storybook/blocks'

import * as HeadingStories from '../stories/Heading.stories'

<Meta of={HeadingStories} />

# Heading

O componente de `Heading` é utilizado para representar títulos e subtítulos em uma página.

<Canvas of={HeadingStories.Primary} />
