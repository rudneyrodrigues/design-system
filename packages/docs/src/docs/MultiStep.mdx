import { Meta, Story, Canvas } from '@storybook/blocks'

import * as MultiStepStories from '../stories/MultiStep.stories'

<Meta of={MultiStepStories} />

# MultiStep

O componente de `MultiStep` é utilizado para guiar o usuário por um processo de múltiplas etapas.

<Canvas of={MultiStepStories.Base} />
<Canvas of={MultiStepStories.Full} />
