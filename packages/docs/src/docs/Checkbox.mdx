import { Meta, Story, Canvas } from '@storybook/blocks'

import * as CheckboxStories from '../stories/Checkbox.stories'

<Meta of={CheckboxStories} />

# Checkbox

O componente de `Checkbox` é utilizado para selecionar uma ou mais opções de um conjunto de opções.

<Canvas of={CheckboxStories.Base} />
